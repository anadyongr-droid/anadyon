# 21 September 2026 — Claude, implementer

**Last verified:** 21 September 2026, Claude.

## The guard written yesterday earned itself overnight

Seven Dependabot pull requests arrived, and three of them split the CodeQL pin
exactly as #83 did on 31 August — `init` moved to v4.38.1, `autobuild` and
`analyze` left on v4.37.9. `lib/actionPinParity.test.ts`, written yesterday for
this, failed on all three and named the mismatched lines. CI refused them
without anyone looking.

That is the difference between fixing an instance and fixing a class. Yesterday
it took a person reading a diff; today it took nothing.

**Replaced by #155**, all three steps on `1c5b675653bb5c22dbe9b12b556ec555138e09fd`.

### A verification trap worth not falling into twice

`v4.38.1` is an **annotated** tag, and that changes what a lookup returns:

```
c23de5a82f64bb08c6d9f28844551440ca298e76    refs/tags/v4.38.1
1c5b675653bb5c22dbe9b12b556ec555138e09fd    refs/tags/v4.38.1^{}
```

The first is the tag object; the second is the commit, and the commit is what a
workflow pins. An exact-ref query for `refs/tags/v4.38.1` returns **only the
first** — which looks exactly like Dependabot proposing a SHA that disagrees
with the tag, the precise shape of a supply-chain red flag, when nothing is
wrong. I very nearly reported it as one.

Yesterday's v4.37.9 check happened to use a pattern matching both lines, so the
distinction never came up. **Peel the tag before concluding anything from a
mismatch.**

## The rest of the queue

- **#148 merged** — four minor/patch bumps: Playwright 1.63.0, jsdom 30.1.0,
  supabase CLI 2.117.0, and `eslint-config-next` 16.3.5, which brings it level
  with the installed Next 16.3.5.
- **#151 declined and closed** — `@types/node` ^26 again, the same offer as #85.
  Types ahead of the runtime is the one direction that fails in production
  rather than at compile time.
- **#149 left open** — `@vitest/coverage-v8@5.0.1` peer-requires `vitest:
  "5.0.1"` exactly, and we run 4.1.11. It needs a vitest major, which is a
  deliberate change of its own. Nothing is blocked: per `AGENTS.md` coverage is
  intentionally not a gate.
- **#150 left open** — eslint 10. The peer ranges permit it, so I installed it
  and ran the lint rather than stopping at the metadata:

  ```
  TypeError: Error while loading rule 'react/display-name':
    contextOrFilename.getFilename is not a function
      at node_modules/eslint-config-next/node_modules/eslint-plugin-react/...
  ```

  ESLint 10 removed `context.getFilename()` and the `eslint-plugin-react`
  bundled inside `eslint-config-next` still calls it. Total lint failure, not a
  few new findings. Same shape as #87: a transitive dependency of
  `eslint-config-next` sets the ceiling.

## Stopping the offers rather than declining them weekly

**#156** changes `.github/dependabot.yml`:

- **Action updates are grouped into one pull request.** The CodeQL split is not
  a bug Dependabot will stop having — it enumerates each sub-path as its own
  dependency by design. Grouping stops it being offered. The test stays, because
  configuration can be changed by anyone and the test is the thing that fails.
- **`@types/node` majors are ignored**, with the reasoning in the file. It is
  the one dependency where newer is the wrong direction, and it had already been
  offered twice in two days. Majors now get raised deliberately alongside the CI
  pin and the Vercel setting — what M-05 asked for and what **E12** waits on.
  Minor and patch still arrive normally.

## Still broken, still needing Tasos

**E16 — there is no off-site backup.** The scheduled run failed at 01:17 with
the same `failed to parse connection string: [REDACTED_DATABASE_URL]`, so that
is two consecutive failures. The rotated `SUPABASE_DB_URL` is malformed, almost
certainly an unencoded character in the password. Last good backup: 16:17 on
20 September, before the rotation.

The redaction held again, which is the one part working as designed.

**F2** — the motorbike insurance expiry passed on 11 September with no record of
renewal, and the fleet check cannot catch it because F1's dates were never
entered. **E6** — the Plesk certificate is now ~22 days out.

## Left unverified

The container restarted mid-session during `verify:fast`; it was re-run clean
afterwards (104 files, 1112 tests) and the dependency tree was checked rather
than assumed — eslint 9.39.4, typescript 6.0.3, @types/node 24.13.6.

Still no way to check any Vercel-side claim from this environment, nor GitHub
secret-scanning alerts or secret metadata: both API paths return 403 through
the proxy. Recorded as not run, per §8.
