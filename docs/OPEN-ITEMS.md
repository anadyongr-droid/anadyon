# Open items

**Last verified:** 25 September 2026, Claude (E19 opened — the third-party crawl-block report was checked against the live site and disproved).

**Read this first, every day.** [`DEFINING-STATEMENTS.md` §12](../DEFINING-STATEMENTS.md)
makes it obligatory for every agent, before picking up a task.

Everything known to be outstanding lives here until it is done or deliberately
dropped — and dropping one is recorded with a reason, not deleted. Claude
curates it daily as part of §11.2, but **any agent may add an item the moment it
is noticed**. An item remembered at the close of day is an item that may not be.

Every item has an **owner**. `Agent` means any agent may take it. `Tasos` means
it needs a person, and the item says exactly what is needed.

---

## 📋 Fleet records — the system is built and waiting for this data

| # | Item | Owner |
|---|---|---|
| F1 | **Record insurance contracts, KTEO and kilometrage in the system for every active vehicle.** Insurer, policy number, insurance expiry, KTEO expiry and odometer, for all 29 vehicles. | **Tasos** |
| F2 | **DATED — the motorbike policies' recorded expiry has passed, and nothing here says they were renewed.** [`INSURANCE-COVER-AND-RESTRICTIONS.md`](INSURANCE-COVER-AND-RESTRICTIONS.md) §1 records **both motorbikes expiring 11 September 2026** and the car *"three weeks later"* — about **2 October**. Today is 20 September, so the motorbike date passed **nine days ago** and the car's is **~12 days away**. Raised under `DEFINING-STATEMENTS.md` §12: *a dated item that has passed is escalated, not silently carried forward*. **This is not an assertion that the bikes are uninsured.** Tasos confirmed on 2 September that he handles renewals directly and they are very likely done — what is missing is any record of it, which is the whole problem. **Why it cannot be caught automatically:** F1's dates were never entered, so `lib/fleetStatus.ts` scores those vehicles `unknown`, and `rentalBar` bars only on `expired`. A vehicle with no insurance date on file **rents today with no statutory check at all** — the one case where the machinery is silent is exactly this one. And per §1 of that document, Law 4261/2014 art. 169 gives **no automatic renewal, no notice, and dissolution on late premium**. **Tasos: confirm the three policies are current and give the new expiry dates**; entering them against the vehicles (F1) closes this permanently and switches on a check that already exists and is already tested. | **Tasos** |

**Nothing has to be built first — and that is the point.** Migration 011 already
added `insurance_provider`, `insurance_policy_no`, `insurance_expiry`,
`kteo_expiry` and `odometer_km` to `vehicles`; the admin vehicle modal already
has an input for each; `lib/fleetStatus.ts` already warns 30 days ahead; and
`/api/admin/vehicles/availability` already refuses to rent a vehicle whose KTEO
or insurance has lapsed — measured against the **pick-up date**, so a booking
taken in March for August is judged on August. `fleetStatus.ts` is explicit that
an expired KTEO voids insurance cover and is an absolute bar, not a warning.

**But it is all inert until the dates are entered.** `rentalBar` bars only on
severity `expired`, and a date that has never been recorded is severity
`unknown` — which does not bar. So a vehicle with no insurance date on file
rents today with **no statutory check at all**. Entering the data is what
switches on protection that already exists and is already tested.

Verified against the code 2 September 2026, not assumed.

## 🚧 Blocking — work cannot proceed correctly until these are answered

| # | Item | Owner |
|---|---|---|
| B6 | **Does the rental use class displace Article 18's declaration requirement?** The Intersalonica terms say that if an under-23 driver's age was not **declared and paid for at inception**, the Company **bears no liability to indemnify** — remedied only by an immediate endorsement at 60% of premium (private licence) or 30% (professional). A rental fleet cannot know its renters at inception. Either the rental use class displaces this, or **every under-23 rental needs a Πρόσθετη Πράξη before the keys move**. B1 does not answer it: unnamed-driver cover and Article 18's declaration duty are different things. **Article 19 does the same for a driver whose first licence is under twelve months old**, at 60% regardless of licence type — so two undeclared loadings may be live, and a young new driver appears to engage both. Ask also whether they compound. Computed cost on our own certificates is **€16.49** (50cc) and **€21.59** (125) per three-month policy at 60%, likely ~1.5× that once duties and taxes are added — but ask the broker for the figure rather than pricing against ours. **The most urgent question on this list.** `INSURANCE-COVER-AND-RESTRICTIONS.md` §2c. | Tasos |
| ~~B0~~ | ~~Widen the cloud environment's network access.~~ **Done 2 Sep** — Tasos set the environment to Custom. Both insurer domains now resolve; the Intersalonica booklet was downloaded and read. Kept one day so it is not re-raised. | Tasos asked on 2 Sep for agents to be able to reach sites useful to the build. **No agent can do this** — it is an account-level setting on the cloud environment (`env_013nFW6rrddhGWTxV1ZRsshx`, "Default", currently **Trusted**), enforced by an egress proxy outside the container. Route: [claude.ai/code](https://claude.ai/code) → cloud icon above the message box → hover the environment → settings gear → **Network access**. Choose **Custom**, list one domain per line, and keep **"Also include default list of common package managers"** checked. Recommended over **Full** — see `INSURANCE-COVER-AND-RESTRICTIONS.md` §7 for the suggested list and the reason. Takes effect for **new** sessions. | Tasos |
| B2 | **Obtain the exact Euroins terms issued with the car policy.** Agent retry completed 20 Sep: Euroins's live HTTPS certificate is expired, but the official documents remain available through the search index. The rental-car IPID dated 19 May 2025 and the 68-page general/special motor terms dated 25 Apr 2024 were located and read at their official `euroins.gr/uploads/` URLs. They confirm that rental-car roadside assistance has its own terms and that the licence-category exclusion applies. They do **not** prove which dated booklet was incorporated into policy 9190600829: the product sheet expressly says it does not replace the policy or its attached terms, and Euroins lists 2022, 2024 and 2025 editions. Ask the broker for the booklet/version delivered with that policy. Until then the car's exact contractual terms remain unknown. | **Tasos / broker** |
| B5 | **Obtain the own-damage policy.** Confirmed to exist on 2 Sep, never seen here. Needed answers: which policy carries it, what it covers, **what excess it leaves**, and whether the excess changes for a young driver. **The €5.00/day Full Damage Waiver — price verified on the live Rates screen 19 September — is sold against terms nobody here has read**, so W2 is blocked on this document rather than on drafting. | Tasos |
| B7 | **Two clauses in the signed contract need counsel.** Article 16 grounds data rights in **Law 2472/1997**, repealed and superseded by GDPR and Law 4624/2019, and bundles consent to direct marketing into the rental — not valid consent. Article 15 gives exclusive jurisdiction to the Zakynthos courts, doubtful against EU-domiciled consumers. `CONTRACT-VS-WEBSITE.md` §8 and §9. | Tasos |
| B4 | **Gate 0 — counsel and the accountant.** Two forwardable briefs, already written. **Now carrying one more question:** article 6β of PD 237/1986 is written as an exclusion, but the compulsory scheme also protects the injured third party — does an excluded claim mean the insurer refuses to pay, or pays the victim and then recovers from us (δικαίωμα αναγωγής)? The difference is whether an excluded claim is a loss borne elsewhere or a debt owed by Anadyon. | Tasos |

## 🗄 Migrations written and awaiting application

Per `AGENTS.md`, agents never apply migrations. Each has a byte-identical copy
under `supabase/migrations/paste/`.

| # | Migration | Status | Owner |
|---|---|---|---|
| M1 | **042** — check-in finalisation | Merged; applied and verified on staging, production pending | Tasos |
| M2 | **043** — handover correction and voiding | Merged in #119; applied and verified on staging, production pending | Tasos |
| M3 | **044** — insurance surcharge rate row | Merged in #118; applied and verified on staging, production pending | Tasos |
| M4 | **045** — grant the four handover gateways | Merged in #119; applied and verified on staging, production pending | Tasos |
| M5 | **046** — grant `service_role` on the nine tables created after 023 | **DATED — apply before 30 October 2026.** Written 23 Sep, not yet applied anywhere. Production is unaffected either way: those tables already hold the grant Supabase gave them automatically. What it protects is every database built by *replaying* the migrations — the staging reset, `supabase db reset`, a new project, a preview branch — which after 30 October would create `vehicle_blocks`, `vehicle_change_requests` and the seven handover tables with no privileges, leaving the whole of check-out and check-in unreachable by the application. Applying it early costs nothing and is a no-op on a database that already has the grants. Section 14 of [`STAGING-AND-OBSERVABILITY-RUNBOOK.md`](STAGING-AND-OBSERVABILITY-RUNBOOK.md). | **Tasos** |

---

## 🔨 Build work

| # | Item | Owner |
|---|---|---|
| W11 | **Website says theft and CDW are included; nothing provides either.** `legal.ts` §6 lists "Theft insurance" and "Collision Damage Waiver (CDW)" as included in **all** rentals. The paper contract sells both as paid options with a non-waivable excess, and **no certificate carries either**. Customer-facing changes were drafted on 19 September and rolled back at Tasos's request because the wording had not been discussed or approved. `CONTRACT-VS-WEBSITE.md` §1. | Tasos to approve wording, then Agent |
| W12 | **Publish the exclusion list.** The contract's front page states in capitals that no insurance covers tyres, mirrors, glass, key loss or theft, the underside or the interior. Customer-facing wording requires Tasos's approval before publication. §4. | Tasos to approve wording, then Agent |
| W13 | **Qualify roadside assistance.** Website §10 promises "free 24-hour roadside assistance" unqualified; the 50cc has none. Customer-facing wording requires Tasos's approval before publication. §5. | Tasos to approve wording, then Agent |
| W14 | **Replace existing printed contract stock with the corrected template.** Article 4(f) incorrectly names Article 10; the maintained printable template already says Article 8. The remaining action is physical, before the next form is printed or used. §9. | **Tasos** |
| W18 | **Fleet photographs are far smaller than §1 requires, and §1 claims otherwise.** `DEFINING-STATEMENTS.md` §1 states "In practice: sources at ~1600px" and reasons about "a 288px card never being sent a 1600px file". Measured 20 September: `fiat-panda.jpg` is **300×185**, `giant-bike.jpg` **516×387**, `hero-zakynthos.jpg` **1200×800**. A 300px-wide source for a car on a retina card is exactly the "soft or muddy vehicle photograph" §1 says costs a booking. **Two ways to resolve and they are not equivalent:** re-shoot or re-source the fleet images at ~1600px so the principle is met, or amend §1 to state the real standard. The first costs photography and is a product-quality decision; the second lowers a bar Tasos set. Left to him rather than corrected in the document, because correcting the document is the option that silently accepts worse photographs. §1 was not edited pending that call — it currently describes a standard the assets do not meet. | **Tasos** |
| W16 | **Work the Search Console 404 export when it arrives (E8).** The archive pass is done — 46 redirects now cover the 86 old URLs that were 404ing (`lib/legacyRedirects.ts`). What the export adds is the class the archive cannot show: links to URLs that were *never* ours — a typo, a truncation, a guessed path — plus a measure of what the redirects actually recovered. For each: redirect if there is a sensible destination, and add it to `docs/inbound-links.json` either way. [`INBOUND-LINKS.md`](INBOUND-LINKS.md). | Agent |
| W1 | **Photo upload saga** — the last piece of phase 2. The schema and private bucket exist, but no upload API/UI does. Do not ship a partial upload: blueprint §4.2a requires the actual Storage objects, metadata and hashes in the nightly encrypted backup and a tested hash-preserving restore first. The current backup saves database rows only. | Agent; hosted restore proof needs backup credentials |
| W2 | **Content correctness against the insurance policies.** Customer-facing corrections for theft/CDW, exclusions and 50cc roadside assistance were drafted on 19 September but rolled back because Tasos had not approved the wording. FDW wording is additionally blocked on B5, the own-damage policy's terms and excess. | Tasos to approve; Agent after approval/B5 |
| W8 | **Our surcharge age and the insurer's age are computed differently.** Article 18 counts age **from 1 January of the year of birth** (`pickupYear − birthYear`); `lib/rentalPolicy.ts` computes true age on the pick-up date. Insurer age is always ≥ true age, so **we never undercharge** — but we do charge some customers whose birthday falls later in the year and whom the insurer already treats as 23. A decision about whose definition to follow, not a defect. | Agent |
| W10 | **Capture licence issue date, and check the twelve-month rule at check-out.** Article 19 makes a first licence under twelve months old an undeclared-loading trigger with the same "no liability" consequence as the under-23 clause. Check whether the licence issue date is captured today; if not, it needs to be, alongside the category check in W7. §2c. | Agent |
| W7 | **Licence category is a condition of cover, not counter etiquette.** Article 6β of PD 237/1986 excludes from compulsory cover any damage caused by a driver lacking the licence required for that category — and the Supreme Court has held the article's three exclusions exhaustive. So a renter on a 125 with only an AM licence voids cover by statute. **The Intersalonica booklet hardens this further**: a licence for other vehicle types does not count, and the exclusion applies *even if the missing licence played no part in the accident and even if the driver knew how to ride*. No causation defence, no competence defence. Check what the check-out flow verifies today, and make licence category against machine an explicit gate. §2b and §2c. | Agent |
| W4 | **`discount_rules` `age_surcharge` is dead, redundant, and cannot work as designed — and removing it is a §13 decision.** Re-read in full on 23 September; the earlier summary (charges per rental not per day, parses the band's lower bound, never called by the public quote route) is accurate but understates it. **It has no caller at all** — the only references to `/api/admin/discount-rules/apply` anywhere are generated `.next/` type artefacts. **It cannot distinguish ages within a band:** `driver_age` arrives as one of `DRIVER_AGE_BANDS` — `21–25`, `26–65`, `66+` — and the route does `parseInt(String(driver_age).split("–")[0])`, so with a threshold of 22 the *entire* 21–25 band is surcharged, a 25-year-old included. That is not a parsing bug to patch; a per-year threshold cannot be computed from a five-year band. **A correct implementation already exists:** `lib/pricing.ts` `calcInsuranceSurchargeLine(extras, ageAtPickup, rentalDays)` takes the exact age at pick-up and the day count, which is what §5 means by one implementation. **So the remedy is removal, not repair** — but `age_surcharge` is a rule type an admin can already have created rows for, and prices, discounts and removed products are all named in `DEFINING-STATEMENTS.md` §13. **Tasos: three questions.** Is the `age_surcharge` rule type used by any row in production today? Should it be removed in favour of the working surcharge, or kept and rebuilt against exact date of birth? And is a duplicate, admin-editable pricing lever wanted at all, given §5 says one module neither side can bypass? Nothing was changed pending that answer. | **Tasos** decides, then Agent |
| ~~W5~~ | ~~**Admin frozen panes.**~~ **Closed 20 Sep** — the existing implementation passed all 32 focused checks on iPad WebKit, desktop WebKit, Firefox and Chromium, including the deliberately broken control that proves the instrument detects clipping. No runtime change was justified. [`HANDOVER-ADMIN-FROZEN-PANES.md`](HANDOVER-ADMIN-FROZEN-PANES.md). | Agent |

---

## ⚖️ Legal and compliance

| # | Item | Owner |
|---|---|---|
| ~~L1~~ | ~~**Check whether the ΕΠΑνΕΚ co-funding page must remain online.**~~ **Closed 19 Sep** — Tasos checked the grant paperwork and confirmed the publicity obligation no longer applies. The old URL may remain a genuine 404. | **Tasos** |

---

## 🧭 Decisions not yet taken

| # | Item | Owner |
|---|---|---|
| N1 | **Is FDW a priced self-insurance product, or withdrawn/repriced?** No own-damage cover behind it on any vehicle, across two insurers. Whatever is chosen, its published wording must be exact. Depends on B3. | Tasos |
| N2 | **Motorbike age by licence category** — AM 16, A1 18, A2 20, as Greek law already sets. Largest commercial gain, smallest cost. [`DRIVER-AGE-MARKET.md`](DRIVER-AGE-MARKET.md) §2 and §7. Depends on B2. | Tasos |
| N3 | **Cars at 19 or 20 with a surcharge**, picking up the segment two local operators serve. §3 and §7. Depends on B2. | Tasos |
| N5 | **Motorbike minimum age: 18 or 21?** The **paper contract already says over 18 for motorbikes**; the website says 21 for everything. One of them is wrong. Greek law sets AM 16 / A1 18 / A2 20, so the contract is the one aligned with the law. `CONTRACT-VS-WEBSITE.md` §2. | Tasos |
| N6 | **Reconcile the two product menus and the two cancellation regimes.** The counter form sells CDW, PAI, FTP, airport tax, fuel and damage charges that the website does not; the website sells seats, GPS and the under-23 surcharge that the form does not. And the contract keeps all prepaid rent on early termination while the website offers free cancellation over 24 hours out. §7 and §9. | Tasos |
| N4 | **Adopt a minimum licence-holding period — the contract already has one.** Contract art. 6.1(a) requires a licence **held at least one year**, and Intersalonica Article 19 prices exactly that line. The website states no tenure rule at all. So this is no longer whether to adopt one — it is publishing and enforcing the one we already contract on. `CONTRACT-VS-WEBSITE.md` §3. Original framing:** Intersalonica **Article 19** charges a **60% loading** for a driver whose *first* licence was issued within the previous twelve months, regardless of licence type, floor €14.67 — and if it was not declared, the insurer **bears no liability to indemnify**. Our own insurer prices exactly the twelve-month line the market uses. Decide whether we refuse under-12-month licences, surcharge them, or declare them; then say so in the terms and check it at the counter. `INSURANCE-COVER-AND-RESTRICTIONS.md` §2c. | Tasos |
| N7 | **Review and validate operating-model rules introduced during the retrospective audit.** A 20 September review of the full available history (10 August onward: 457 commits and 86 merged PRs) found seven live rules or workflows for which repository and PR records do not contain sufficiently specific approval from Tasos. Validate, modify or reject each separately: **(1)** blanket minimum age 21 for every vehicle ([commit `81c25d9`](https://github.com/anadyongr-droid/anadyon/commit/81c25d9628fda71ce53f4e7375cf7df3b1a7d872); overlaps N5); **(2)** maximum three baby/child seats combined ([PR #29](https://github.com/anadyongr-droid/anadyon/pull/29)); **(3)** substitution rules — free upgrades without permission, downgrade only with agreement and lower price, transmission changes only when customer-requested, and no cross-family swaps ([initial commit](https://github.com/anadyongr-droid/anadyon/commit/e4e6d4ec3ada5042b8b98ac9aeae2392d572e5d3), [PR #12](https://github.com/anadyongr-droid/anadyon/pull/12), [PR #39](https://github.com/anadyongr-droid/anadyon/pull/39)); **(4)** automatic eligible-vehicle assignment for website requests ([PR #12](https://github.com/anadyongr-droid/anadyon/pull/12)); **(5)** promo entitlement lifecycle — hold at quote confirmation, redeem on payment, release on expiry/cancellation/void ([PR #29](https://github.com/anadyongr-droid/anadyon/pull/29)); **(6)** the public promise “No hidden fees — the quoted price is what you pay” ([PR #21](https://github.com/anadyongr-droid/anadyon/pull/21)); and **(7)** every website quote automatically creating a linked customer and Pending reservation ([PRs #9](https://github.com/anadyongr-droid/anadyon/pull/9), [#11](https://github.com/anadyongr-droid/anadyon/pull/11), [#12](https://github.com/anadyongr-droid/anadyon/pull/12)). Absence of repository evidence does not prove that no private-chat approval occurred, so this is a validation task, not a conclusion of wrongdoing. **No agent is to alter these behaviours merely because they are listed here:** keep the current state until Tasos decides each point, then record each decision and only implement explicitly approved changes under `DEFINING-STATEMENTS.md` §13. | **Tasos** |

---

## 🔁 Repository hygiene

| # | Item | Owner |
|---|---|---|
| ~~R1~~ | ~~`lib/stripe.ts` on `main` does not typecheck on a clean checkout.~~ **Wrong, withdrawn 14 Sep.** `main` typechecks fine: CI installs with `npm ci`, which honours the lockfile's **stripe 22.5.0**, and the pinned `2026-07-29.dahlia` is correct for it. The error only appeared in an **agent sandbox that had run `npm install`**, resolving the caret in `package.json` to 22.6.0 and reporting the mismatch *inverted*. The pin was then "fixed" to match the sandbox on three branches, which is what actually broke CI on #107. **The remedy for a local mismatch is `npm ci`, never editing the literal.** Recorded in `lib/stripe.ts` as a comment so the next agent does not repeat it. **Still to clean up:** `claude/insurance-surcharge` carries the same bad edit and will fail CI until reverted. |
| ~~R2~~ | ~~**Dependabot backlog.**~~ **Closed 20 Sep — six of seven done, the seventh blocked upstream.** Security triage earlier that day merged #86, #108, #106, #103, #104 and #101; `npm audit` on `main` reports **0 vulnerabilities**, so nothing remaining was a security fix. The seven maintenance PRs were then assessed individually rather than merged to empty the queue, and **four were wrong as offered**: #83 bumped one of three CodeQL entry points that must share a tag; #85 offered `@types/node` ^26 against a Node 24 runtime; #78/#79/#80 named three node20 actions while a fourth, `actions/cache`, was node20 with no PR at all. Replaced by **#130, #131, #132 and #133**, all merged, each with the SHA resolved from the upstream tag and each target's `action.yml` read at that SHA. `lib/actionPinParity.test.ts` now fails on a split pin. **No node20 action remains anywhere in the repository.** **#87 (TypeScript 7.0.2) stays open and is blocked upstream, not by us** — `tsc` passes; `typescript-eslint` refuses to load against TS 7 and caps at `<6.1.0` two levels down through `eslint-config-next`. Tracked at typescript-eslint#10940; it becomes mergeable on its own. **Do not close it.** | Agent |
| R7 | **Two agents plus `Require branches to be up to date` means merges must be serialised.** Branch protection is strict: a pull request whose base has moved is refused with `405 Required status check "build" is expected`, which reads like a missing check rather than a stale branch — the real signal is `mergeable_state: behind`. Every merge therefore needs update-branch, a full CI run, then merge, and **anything merged in between puts the next one behind again**. Hit four times on 20 September while Codex merged #134 in parallel. Not a fault and not worth relaxing — strictness is why `main` stays green. The practical rule: **check `mergeable_state` before concluding a check is missing**, and expect one CI cycle per merge when both agents are active. | Agent |
| ~~R8~~ | ~~**TypeScript: 5.9.3 today, 6.0.3 available.**~~ **Closed 20 Sep** — `typescript@6.0.3` merged as #140 with Tasos's go-ahead. Typecheck clean, 0 lint errors with the same 24 pre-existing warnings, 101 files / 1076 tests. **No runtime effect**: TypeScript is a build-time checker run as `tsc --noEmit`, and the deployed bundle comes from Next.js's own toolchain. **#87 (TypeScript 7.0.2) stays open and must not be closed** — 7.0 is the native Go port and ships *without a programmatic API*, so typescript-eslint, Volar and Angular's template checking are all locked out; a new API is promised for 7.1. [typescript-eslint#10940](https://github.com/typescript-eslint/typescript-eslint/issues/10940) has no milestone and names ESLint's lack of async parser support as a blocker, calling the port "many months away from being stable". 6.0 is the staging post: 7 adopts its defaults and turns its deprecations into hard errors. | Agent |
| ~~R3~~ | ~~**Four stale PRs.**~~ **Closed 20 Sep** — #16, #58 and #71 were closed with reasons recorded. #31's accurate self-resolved incident closure was carried onto `main` by #121, then #31 was closed as superseded. History remains in the closed PRs. | Agent |
| ~~R4~~ | ~~**`codex/incident-admin-middleware-timeout` had no PR.**~~ **Closed 20 Sep** — its useful incident record was already on `main`; #31 supplied the missing outcome and recurrence instructions through merged #121. The obsolete remote incident branches were deleted after the merge. | Agent |
| ~~R5~~ | ~~**PRs #95 and #98 awaited disposition.**~~ **Closed 20 Sep** — #95 was superseded by merged #119; #98's sandbox-disk guide reached `main` through #121, after which #98 was closed and its obsolete remote branch deleted. | Agent |
| ~~R6~~ | ~~**`claude/insurance-surcharge` had no PR.**~~ **Closed 19 Sep** — the migration and corrected Stripe pin merged as #118. | Agent |

---

## 🏗 Environment and tooling

| # | Item | Owner |
|---|---|---|
| E6 | **Reissue the Plesk certificate for only the hosts still on that server.** Papaki reports Let's Encrypt renewal failing for `anadyon.gr` and `*.anadyon.gr`, **~23 days to expiry** (the figure read 29 days when recorded on 14 September; recomputed 20 September, so expiry falls around **13 October 2026**). Cause, inferred from DNS: `anadyon.gr` and `www` now resolve to **76.76.21.21 (Vercel)**, so the HTTP-01 challenge no longer reaches Plesk and can never succeed. Vercel issues and renews the website's certificate itself — nothing needed there. But the wildcard is what secures **`mail.anadyon.gr`** (213.158.90.117, Papaki), so staff mail clients start warning if it lapses. **Fix in Plesk:** reissue for `mail.anadyon.gr` and the webmail/panel host only, dropping `anadyon.gr` and the wildcard. Booking confirmations unaffected — they go via Resend. Which hostnames that certificate is actually bound to could not be verified from here; the session's egress proxy re-terminates TLS, so `openssl` returns the proxy's certificate, not the real one. `EMAIL-DELIVERABILITY.md`. | **Tasos** |
| E9 | **DMARC: do NOT tighten `p=none` yet — a real failing source was found.** Three reports read 19 September (`docs/dmarc/`, `npm run check:dmarc`). Four of five sources are grserver's relay pool (relay11/14/15, IPv4 and IPv6) and all pass SPF and DKIM aligned. The fifth, `mail-sor-f41.google.com`, is a **Gmail forwarder that failed DMARC outright** — SPF passed for `gmail.com` (unaligned), no DKIM survived the forward. Under `p=quarantine` that message goes to spam, and `customerservice@anadyon.gr` forwards to Gmail, so this hop is live. **Next:** collect a week or two across all five receivers; establish whether that forwarded mail reaches customers or only the internal Gmail copy; if tightening, ramp with `pct=10` first. Reports were being deleted unread and Gmail purges its bin after 30 days — the durable fix is pointing `rua=` at a parsing service (Postmark's is free; `rua` takes a comma-separated list so the mailbox copy can stay). [`EMAIL-DELIVERABILITY.md`](EMAIL-DELIVERABILITY.md). | Agent + **Tasos** (save reports before they purge) |
| E10 | **Add a CAA record — after E6, not before.** The domain has none, so any CA may issue for it. Normally a cheap hardening step, but the Plesk Let's Encrypt renewal is *already* failing (E6), and a CAA record set slightly wrong blocks issuance for Vercel and Plesk at once. Sequence matters: fix E6, confirm both certificates renew, then add CAA. | Agent (with Tasos at the registrar) |
| E5 | **Naver has blocked the office mail relay — and it is costing bookings now, not hypothetically.** `relay12.grserver.gr` (`88.99.38.195`, shared Hetzner range) is refused by `mx4.mail.naver.com` with `421 4.3.2 Your ip blocked`, ref `VPdBxVRJReWvU4oJvox9JA`. Refused at connection time, before authentication, so SPF, DKIM and content play no part. **Nothing is misconfigured on our side** and only grserver can request delisting. **A quote request from a `naver.com` customer with a Korean mobile arrived on 18 September for a next-day pick-up** — any staff reply from the mailbox would have bounced, unseen by the customer. Ask grserver for delisting, and until it clears see the Korean-customer workaround in [`EMAIL-DELIVERABILITY.md`](EMAIL-DELIVERABILITY.md). | **Tasos** |
| E8 | **Confirm whether `anadyon.gr` is verified in Google Search Console, then export *Indexing → Pages → Not found (404)* and the Links report.** Step-by-step in [`INBOUND-LINKS.md`](INBOUND-LINKS.md). Checked 19 September: no `google-site-verification` meta tag and no DNS TXT beyond SPF, which rules out two methods but not the Analytics one, so it may already be connected. **If it is not:** use a *URL-prefix* property (`https://anadyon.gr`) and the Google Analytics method — GA is already live under the same Google account. The Analytics method does **not** work for a *Domain* property; Google's docs say DNS is "the only way to verify a Domain property", and adding a DNS record is the registrar trip that E4 already needs. Note the limits before setting expectations: the 404 report covers **only the past month**, and the Links report is a **sample capped at 1,000 rows** — this is a discovery feed for what is still breaking, not a historical audit. Drop the CSVs in `docs/` and an agent takes it from there. Free and worth the same trip: Bing Webmaster Tools and Ahrefs Free. **E19 needs the same visit** — one URL Inspection while you are already there. | **Tasos** |
| E1 | **Sentry project** — needs a dashboard. [`STAGING-AND-OBSERVABILITY-RUNBOOK.md`](STAGING-AND-OBSERVABILITY-RUNBOOK.md). | Tasos |
| ~~E2~~ | ~~**Staging reset from main.**~~ **Closed 19 Sep** — the guarded reset passed twice against project `fzycvstifmltxybffinq`, replaying all 44 migrations with stable synthetic fixtures, Auth roles, grants and schema checks. | Tasos |
| E3 | **Remaining staging parity and §8 acceptance.** The expected-difference schema checker is implemented with a narrow 042–045 manifest; its live read-only production/staging run remains unverified. The application branch is synced and its stable deployment verified. Deployment identity is implemented, with hosted acceptance pending merge/deploy. Continue `STAGING-AND-OBSERVABILITY-RUNBOOK.md` §13 with the deployed-browser journey, configuration shape, policy-neutral fixtures, then scheduled drift checks. Hosted vendor and Sentry acceptance remain dependent on account setup. Record every unrun check as unrun, never as passed. | Agent |
| E11 | **A cancelled staging E2E check is not a passing one, and it looks benign.** The `End-to-end against staging` job declares `concurrency: { group: anadyon-staging-e2e, cancel-in-progress: false }` so two runs never touch shared staging at once. GitHub keeps only **one** run pending per group, so when a third arrives the one already queued is **cancelled** — with two pull requests open at the same time, the earlier one's E2E dies. Seen on #131 on 20 September. The serialisation is right and should stay; the hazard is that the check goes **grey, not red**, so a PR with an E2E that never ran does not read as failing. Per §8 that is **not run**, never passed. **Until something better exists, check the E2E conclusion explicitly before merging and re-run a cancelled one.** Worth designing a real fix — a queue that waits rather than cancels, or a required check that treats `cancelled` as unsatisfied. | Agent |
| E12 | **Which Node major Vercel actually runs has not been verified since 19 August.** CI is pinned to 24 and `@types/node` was matched to 24 on 20 September (#131), both on the audit's observation that production served Node 24. That observation is now a month old and cannot be checked from an agent session — it is a Vercel dashboard reading. **Nothing is broken if it is still 24, and `^24` is not ahead of anything later**, so this is confirmation rather than a fault. If Vercel has moved, raise CI, `@types/node` and Vercel together as one change, which is what audit M-05 asked for. **Tasos: Vercel → Project → Settings → Build & Deployment → Node.js Version; report the number.** | **Tasos** |
| ~~E13~~ | ~~**Rotate the production Supabase database password and update every stored connection URL.**~~ **Closed 20 Sep** — Tasos reset the production password, updated the local parity URL, password-manager copy and GitHub Actions `SUPABASE_DB_URL`; GitHub metadata confirmed the repository secret update. Production authenticated and produced a read-only schema dump afterward. Staging authentication is a separate E3 acceptance issue: its URL was never exposed and its saved password differs from production. | **Tasos** |
| ~~E14~~ | ~~**The staging end-to-end suite failed once, non-deterministically, and the cause is NOT established.**~~ **Instrumented 23 September; the underlying non-determinism is still unexplained.** `makeReservation` in `03-lifecycle.e2e.ts` returned `(await res.json()).id` with no status check, so a failed create returned `undefined` and every later step addressed `/api/admin/reservations/undefined` — turning one failure into four 404s reported against the lifecycle routes, which is not where the fault was. It now asserts the status and the id, carrying the response body, which is the pattern `05-operations.e2e.ts` already used and the reason its `reservations_customer_id_fkey` error was legible while phase 3's was not. **This does not fix the flake and does not claim to** — it makes the next occurrence name its own cause on the first run instead of the third. The disproved parallel-cleanup theory stays recorded: `vitest.e2e.config.ts` sets `fileParallelism: false` and the run's timestamps show the files ran in sequence. | Agent |
| ~~E16~~ | ~~**The nightly off-site backup is failing.**~~ **Closed 21 September.** The `SUPABASE_DB_URL` secret began `postpostgresql://` — four stray characters on the front of an otherwise correct Supabase session-pooler URI, so the CLI rejected the string before ever authenticating. Tasos re-pasted it fresh from the dashboard and a dispatched run ([35596067151](https://github.com/anadyongr-droid/anadyon/actions/runs/35596067151)) passed every step: `roles.sql` 370 B, `schema.sql` 113,371 B, `data.sql` 600,154 B, a 142 KB encrypted archive that decrypted and listed cleanly, uploaded to R2, and the prune rotated the 21 August daily out. **The backup gap ran from 16:17 on 20 September to 11:50 on 21 September** — four failed runs, three of them the malformed credential and one the missing checkout fixed by #146. Nothing was lost; the 20 September 16:17 archive covered the window. **Two guesses preceded the measurement and both cost a round trip**, which is why `describeDatabaseUrlShape` exists (#159, #160): the next such failure names its own cause on the first run. The log of the successful run contains no credential material — verified, not assumed. | Agent |
| E15 | **Finish retiring Preview deployments built before the credential-scope correction.** The 20 September live Vercel audit found general Preview deployments inherited production Supabase, Stripe, Resend, Gmail, Twilio, Telegram, AADE, Anthropic and Apify credentials. All general Preview access was removed while Production and the staging branch's three isolated Supabase values were preserved. New code adds a fail-closed build boundary, credentialless non-staging compilation and lazy Resend initialisation. After that change reaches `main` and `staging`, verify the new staging deployment, delete every older Preview artifact that retains the former environment snapshot, then rerun 84/84 hosted checks. Do not rotate unrelated vendor credentials absent evidence of disclosure; the issue was over-broad availability, not a logged value. | Agent |
| E17 | **The backup's Telegram alert has never been able to send anything — the GitHub secrets do not exist.** Proven 21 September by dispatching the new `alert-test.yml`, which failed with `Cannot send the Telegram alert — missing: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID`. The old step began `[ -z "${TELEGRAM_BOT_TOKEN:-}" ] && exit 0`, so every run exited **0 having sent nothing**, and four failed backups in 24 hours each reported that step successful. Tasos confirmed he has received no Telegram alert since the move off Make.com. **#163 makes a failure to deliver fail the step**, so this cannot hide again — but the secrets still have to exist. **Three things to check, and they are in two different places:** (1) **GitHub → Settings → Secrets and variables → Actions** — add `TELEGRAM_BOT_TOKEN` and `TELEGRAM_CHAT_ID`; this is what the nightly backup uses and it is confirmed empty. (2) **Vercel → Project → Settings → Environment Variables** — the *application* alerts (booking notifications, the morning briefing, the Stripe webhook) read the same two names from there; that environment is not visible from an agent session, so whether they are set is **unverified**. (3) **Supabase → Table editor → `alert_outbox`** — `lib/telegram.ts` queues every undelivered alert with the reason in its `error` column, so if the application path has also been failing, the rows say exactly why. **One thing to look out for:** `lib/telegram.ts` falls back to a hardcoded chat id, `-1003920236402`, when `TELEGRAM_CHAT_ID` is unset — so a missing variable there does not fail, it posts to whatever that chat is. After the secrets are added, dispatch **Test the Telegram alert** and a green run means a message genuinely arrived. | **Tasos**, then Agent |
| ~~E18~~ | ~~**Every health check reports through Telegram and nothing else.**~~ **Closed 21 September.** The six checks in `lib/healthChecks.ts` now have a surface that can be *looked at*: **Admin → Settings → Site health**, backed by `GET /api/admin/health`. It runs them **live** rather than returning a stored snapshot — which answers the better question, "is anything wrong now?", and is what made it shippable today, since a stored history needs a table and agents do not apply migrations. Admin role required, checked in the route as well as by `proxy.ts`, and a refused caller does not trigger the checks at all. Run on a button rather than on page load: each run makes six live requests to production, one of them posting to the Resend webhook. **Telegram delivery is still worth fixing (E17)** — this removes the single point of failure, it does not replace the push. | Agent |
| E19 | **Confirm Google can actually crawl and render `anadyon.gr` — one URL Inspection, on the same Search Console visit as E8.** Raised 25 September after a third-party SEO tool (Muse AI) reported the site returns `LIVE_CRAWL_POLICY_BLOCKED` and blamed *"your Java WAF / security policy… likely ModSecurity or Spring Security"*, warning that *"if I'm blocked, Googlebot might be partially blocked too"*. **Checked against the live site the same day, and every specific claim is false.** `https://anadyon.gr/` returns **HTTP 200, 34,046 bytes** to desktop Googlebot, mobile Googlebot, `Chrome-Lighthouse` and plain curl alike; `server: Vercel`, not Java — there is no Java in this project. The live HTML contains **no** `noindex` and **no** `wp-content`; there is no `X-Robots-Tag`. `robots.txt` already returns exactly what the tool recommended (`User-Agent: *` / `Allow: /`, disallowing only `/admin/`, `/api/`, `/quote/`, `/el/quote/`) plus a `Sitemap:` line the tool did not notice, and `sitemap.xml` is live with **26 URLs**. `www.gstatic.com` is already in the CSP `script-src`. The tool was almost certainly reading a cached copy of the **old WordPress site** — the `/wp-content/` hero path and the logo filename `anadyon-car-rentals-zakynthos-logo-wide.jpg` it quotes exist nowhere in this repository, whose logo is `public/logo.jpg`, and `lib/legacyRedirects.ts` exists precisely because there was a WordPress site before. **One real mechanism, and it is not the one they meant:** we send `frame-ancestors 'none'`, which stops third-party *thumbnail* scrapers embedding the site in an iframe — deliberate clickjacking protection, and **Googlebot does not crawl through iframes**. Do not relax it. **So: no change is warranted, and none was made.** What is left is confirmation from the only authoritative source, which is a dashboard and therefore not an agent's to read. **Tasos: Search Console → URL Inspection → `https://anadyon.gr/` → Test Live URL** (reports whether Google can fetch and render, with the rendered screenshot), then **Indexing → Pages** for how many of the 26 sitemap URLs are indexed. If either shows a genuine problem, paste what it says and an agent takes it from there. Recorded here so the tool's diagnosis is not acted on, and not re-investigated from scratch when it is repeated. | **Tasos** |

---

## ✅ Recently closed

Kept briefly so a closed item is not reopened by someone who remembers it as open.

| Item | Closed | How |
|---|---|---|
| Admin frozen panes (W5) | 20 Sep 2026 | Existing implementation verified 32/32 across four browser/device engines; no code change. |
| Signed-agreement rationale (W9) | 19 Sep 2026 | Migration 041 and its paste copy now explain why the check-out blocker protects Anadyon from insurer recourse. |
| Feature comparison (W15) | 19 Sep 2026 | Re-scored using staff-reachable production behavior as the definition of ✅; staging-only/API-only work is ⚠️. |
| Empty quote address (W17) | 19 Sep 2026 | Staff notification drops empty address parts and does not turn the form's default country into a fabricated address. |
| Deliberate post-MFA navigation warning (W6) | 19 Sep 2026 | Narrow lint suppression added beside the existing security rationale. |
| RPC staff identity — how does a function know who is calling? | 31 Aug 2026 | Option A adopted, diagnostic run and removed (#94). Production grant gate cleared. |
| Driver-age contradiction between terms, modal and FAQ (audit B1) | before 2 Sep | `lib/rentalPolicy.ts` single-sources all three. The audit file still reads as open. |
| Document upload verified against staging | 31 Aug 2026 | Verified. |
| Young-driver surcharge published but never charged | 2 Sep 2026 | Built at €5/day under 23, `claude/insurance-surcharge`. Migration 044 still to apply — see M3. |
| **Insurance renewals — car, 125 and 50cc** | 2 Sep 2026 | Tasos is handling the renewals directly. The 125 (ΖΒΒ 0565) certificate was the stale copy it looked like: the current policy is **217443636**, 11 Jun → **11 Sep 2026**. Both motorbikes now expire on the same day. Superseded by F1, which is what puts the dates where the system can act on them. |
| **B1 — are renters covered as unnamed drivers?** | 2 Sep 2026 | **Yes**, confirmed by Tasos. The single largest risk in the insurance reading, closed. |
| **B3 — is there own-damage cover we have not been shown?** | 2 Sep 2026 | **Yes**, confirmed by Tasos. The earlier conclusion that FDW was entirely self-insured is **withdrawn**. Replaced by B5: the cover exists, its terms and excess have not been seen, and the waiver's wording depends on them. |
| **Insurance expiry in the vehicle record, with stop-sell** | already built | Found on 2 Sep to exist already — migration 011 columns, the admin modal inputs, `lib/fleetStatus.ts` 30-day warnings, and a hard bar in the availability route measured against the pick-up date. Was listed as open build work in error. What remains is the data, not the code: F1. |
