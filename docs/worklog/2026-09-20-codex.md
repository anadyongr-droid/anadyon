# Codex worklog — 20 September 2026

## Completed

- Reconciled the live backlog and pull requests from current `origin/main`.
- Re-ran the frozen-pane proof: 32/32 across iPad WebKit, desktop WebKit,
  Firefox and Chromium. Closed W5 without changing the interface.
- Closed stale PRs #16, #58 and #71 with reasons recorded and their history
  preserved. Carried PR #31's accurate self-resolved incident outcome and PR
  #98's sandbox-disk guide into the current cleanup branch.
- Confirmed #95 was superseded by merged #119 and the old insurance-surcharge
  branch by merged #118; corrected the stale repository-hygiene records.
- Refreshed fully-green dependency PRs #108, #106, #86, #81 and #79 onto
  current `main` so protection checks run against the real merge base. No check
  was bypassed.
- Merged #86, #108 and #106, then reran the package audit on current `main`.
  The only remaining findings were Browserslist (high),
  baseline-browser-mapping (moderate) and qs (moderate). Refreshed and merged
  #103, #104 and #101 one at a time after each passed the full build, staging
  E2E, CodeQL/dependency review and Vercel deployment. The final audit on
  `f310fbf` reports zero vulnerabilities.
- Did not merge the new 13-package grouped PR #122. It includes a Stripe SDK
  upgrade that changes the SDK's required API-version literal and currently
  fails typecheck; that compatibility decision must be reviewed deliberately,
  not disguised as a mechanical dependency repair.
- Re-read all three saved DMARC reports. The evidence is unchanged: one Gmail
  forwarder fails alignment, so `p=none` must not be tightened yet.
- Retried the Euroins evidence. The live site has an expired TLS certificate,
  but the official indexed rental-car IPID and 68-page public motor terms were
  readable. They do not identify which dated edition was incorporated into
  policy 9190600829, so B2 is now a narrower broker request rather than an
  agent retry.
- Verified the current photo-upload prerequisite. The database schema and
  private bucket exist, but the nightly backup saves database rows and not
  Storage objects. The blueprint forbids shipping evidence uploads until an
  encrypted object backup and hash-preserving hosted restore are proven.

## Verification

- `npm run test:frozen-panes`: 32 passed.
- `npm run verify:fast`: type-check passed, lint passed with the existing 24
  warnings, 96 test files and 1,053 tests passed.
- `npm run check:dmarc -- <three saved reports>`: six messages, five sources,
  one DMARC-failing Gmail forwarder; no change from 19 September.

## Still open or dependent on Tasos

- W1 can be implemented only as a complete evidence path. Hosted backup and
  restore proof need the existing backup credentials; do not deploy a partial
  upload path.
- B2 needs the broker to supply the exact Euroins terms edition attached to
  policy 9190600829.
- E9 needs more aggregate reports and a decision about the Gmail forwarding
  path before any DMARC enforcement change.
- All operating-model/customer relationship decisions remain with Tasos under
  `DEFINING-STATEMENTS.md` §13.

## Staging parity continuation

- Implemented §13 item 2 without touching either hosted database. The existing
  read-only dump comparison now accepts only complete SQL statements explained
  by the declared 042–045 boundary. Migration 044 is explicitly data-only;
  production-only drift, unexplained staging drift and a missing required
  schema effect all fail.
- Added seven focused parity tests. They pass, as do TypeScript and focused
  lint. The full unit run reached 1,001 passing tests but 53 PGlite cases timed
  out while constructing fresh databases on this host; the failures are in
  unchanged handover suites and are recorded as an environment/resource
  result, not represented as product regressions.
- The live read-only comparison was not run: neither database URL is present in
  the isolated worktree. No migration or hosted write was attempted.

## Staging branch sync and deployment identity

- Reviewed the two commits unique to `staging`: the first was an empty stable
  deployment marker and the second an earlier merge from `main`. There was no
  staging-only application change to preserve manually.
- Merged `main` through #134 into `staging` without force-pushing. The exact
  merge passed `npm run verify:fast` (99 files, 1,068 tests), deployed
  successfully through Vercel at `17da564`, and the stable alias served the
  public homepage without browser-console errors.
- Added an admin-only deployment-identity endpoint returning the commit,
  Vercel environment and Supabase project ref. It returns no URL, key or other
  environment value, is not available to staff, and is explicitly non-cacheable.
- Focused endpoint/helper tests, ESLint and TypeScript pass. Hosted acceptance
  of the endpoint remains unrun until its PR is merged and reaches staging.

## Schema-parity credential disclosure and remediation

- The first live parity attempt failed before connecting because the Supabase
  CLI requires Docker Desktop and Docker was unavailable. Node's uncaught
  `execFileSync` exception nevertheless reproduced the complete production
  `--db-url` argument, including its password, in task output. No database was
  reached; the staging URL was never passed to the CLI and was not exposed.
- Replaced inherited synchronous execution with captured `spawnSync` output.
  Known connection strings and any incidental PostgreSQL URL are now removed
  before diagnostics are printed. Docker and the local CLI are checked before
  the script reads either credential.
- Added fail-first regression coverage using fake URLs. The unfixed checker
  failed three credential-safety tests; the remediated checker passes all 11
  schema-parity tests plus a process-level missing-Docker check whose combined
  output contains neither fake URL nor fake password.
- Production database-password rotation and updates to the local parity URL
  and GitHub `SUPABASE_DB_URL` secret remain with Tasos under E13. The live
  parity check remains unrun and must not be retried until rotation is complete.
- Verification: the 11 focused parity tests, focused ESLint and the process-level
  fake-secret check pass. `verify:fast` passed TypeScript and lint, then 31
  unchanged PGlite cases exceeded their 10-second fixture hook while 1,035
  tests passed. Rerunning all four affected files sequentially removed resource
  contention and passed their 109 tests, so no product failure is represented
  as green and no remediation test is hidden by the host limitation.

## Credential-boundary audit and hosted verification

- Tasos completed the production database-password rotation and updated the
  local parity URL, password-manager copy and GitHub Actions `SUPABASE_DB_URL`.
  GitHub metadata recorded the secret update at 18:46:13Z. A subsequent
  read-only production schema dump authenticated successfully; staging then
  rejected its separate saved database password, so live parity remains unrun
  rather than misreported as passed.
- GitHub secret scanning reports no repository alerts, and the locked npm tree
  reports zero known vulnerabilities.
- Live Vercel environment metadata exposed a more important boundary failure:
  general Preview deployments inherited production Supabase service-role/API
  configuration and production Stripe, Resend, Gmail, Twilio, Telegram, AADE,
  Anthropic, Apify and Wise values. No value was read. All production-sensitive
  entries were removed from general Preview while Production values and the
  staging branch's isolated Supabase trio were preserved.
- Added a pre-build credential boundary. General Preview now fails if it receives
  any server credential or Supabase target; staging fails on the wrong Supabase
  project, missing staging keys, production-only integrations, live Stripe or
  `AADE_PRODUCTION=true`.
- Database URL redaction now also covers malformed schemes and arbitrary
  basic-auth URLs. The nightly backup calls a captured/redacted wrapper instead
  of placing the database command directly in workflow shell, and the destructive
  staging reset now captures and redacts child output before printing it.
- Resend is now initialised only when a send is attempted. A missing key no
  longer prevents a clean Preview build; the existing mail outbox path records
  and queues the failed send instead. This removes the architectural pressure to
  give every Preview an email credential.
- The first clean general Preview proved a second import-time dependency:
  `lib/supabase.ts` constructed both clients while Next collected route data.
  Non-staging previews now compile with an unreachable `.invalid` client only
  when all three Supabase values are absent. Partial configuration, production
  and staging still fail closed. The pre-build boundary prevents a real target
  from entering that mode.
- Hosted staging E2E passed 84/84 with synthetic data and forced invalid mail.
  Production smoke checks returned 200 for `/`, `/admin/login`, `robots.txt` and
  `sitemap.xml`; `/en` redirected as designed and the anonymous admin identity
  request returned 401. CSP, HSTS, frame, referrer, permissions and MIME-sniffing
  headers were present. Vercel recorded no production or Preview error/fatal
  runtime logs in the preceding 24 hours.
- Focused credential, mail and parity coverage passes 23/23; TypeScript and
  focused lint pass. The all-at-once unit run again stalled in PGlite setup on
  this host and was stopped without being represented as green. A Webpack build
  compiled successfully and then exceeded the local 2 GB type-check heap; Vercel
  remains the authoritative 8 GB build gate for the pull request.
- Historical Preview deployments keep their original environment snapshot.
  E15 tracks replacing staging under the corrected scope and deleting all older
  Preview artifacts after the replacement and protected checks are green.
