# 2026-09-19 — Codex

## Phase 2 handover reconciliation

Reconciled the stale `claude/phase2-correct-void` branch with `main` in an
isolated worktree. The only textual merge conflict was the rental-system
blueprint; the current 19 September material was retained and the branch's
counter HTTP and correction/voiding decisions were added after it.

The replacement PR #119 delivered migration 043, the five staff handover HTTP
routes, and migration 045. The routes call the four identity-verifying gateways
through the user's cookie-backed Supabase session, so the database sees
`auth.uid()` and checks server-owned role metadata. `anon`, `service_role` and
`PUBLIC` remain unable to execute the gateways; the privileged implementation
functions retain their existing service-role compatibility.

The old grant tests were first run before changing their expectation and failed
only for `authenticated`, proving migration 045 was what changed the result.
After the expectation update, the route suite, TypeScript check and all 44
migrations replayed cleanly.

## Staging activation

- Reconciled the stale handover work into PR #119: migrations 043 and 045 plus
  the handover routes. PR #118 had already merged migration 044 and restored the
  lockfile-compatible Stripe API version.
- Tasos explicitly authorised the destructive **staging-only** reset. The
  guarded reset ran twice against Supabase project `fzycvstifmltxybffinq` and
  passed both times: all 44 migrations, stable synthetic fixtures, admin/staff
  Auth logins and role claims, grant checks, schema checks and the private
  document bucket. Production was not touched.
- Added branch-specific `staging` overrides in Vercel for the Supabase URL,
  anon key and service-role key. Production variables were not changed. Merged
  current `main` into the permanent staging branch without force-pushing and
  deployed the stable staging alias.
- Verified synthetic admin and staff browser logins and enrolled separate MFA
  factors. Verified the staff boundary by requesting `/admin/users`: staging
  redirected the staff session to `/admin/reservations` and rendered no Users
  navigation. The remaining hosted checklist is deliberately still open where
  it was not measured: production-schema parity, booking/mail redirection,
  Stripe, AADE, manual briefing and Sentry raw-event inspection.

## Correctness work

- Removed the English and Greek website claims that theft insurance and CDW are
  included in every rental. Published the verified exclusions and qualified
  roadside assistance for 50cc motorbikes. Optional damage-cover wording now
  defers to the written quote until the own-damage policy in B5 is supplied.
- Fixed quote notifications that rendered an empty customer address as
  `, , , Greece`; the default country no longer becomes an invented address.
- Added the insurance-recourse reason beside migration 041's mandatory signed
  agreement blocker, in both the migration and paste copy.
- Re-scored the blueprint feature table. A green check now means reachable by
  staff in production; merged or staging-only database/API work remains partial.
- Added the narrow lint suppression that protects the deliberate hard browser
  navigation after MFA.
- The production build found a pre-existing Next.js 16 error: the handover
  Route Handler exported non-handler helper functions. Moved the user-scoped
  gateway client and actor lookup to `lib/handoverAuth.ts`; route modules now
  export only supported route entries.
- Recorded Tasos's decision that the ΕΠΑνΕΚ publicity obligation has ended.

## Verification

- 1,055/1,055 unit tests passed across 97 files when run serially. The default
  parallel local run exhausted the constrained runner and timed out four PGlite
  suites; those suites passed 109/109 alone before the complete serial pass.
- Migration replay passed all 44 migrations and the idempotent staging seed.
- TypeScript passed; ESLint had no errors and retained the existing warning
  backlog. The webpack production build compiled, type-checked and generated
  all 94 routes.
- Translation passed 14/14 pages; static accessibility passed 28/28; SEO passed
  60/60; Playwright passed 86 browser checks across Chromium and Firefox, with
  four rate-dependent checks skipped as designed under placeholder credentials.

## Still needs a person or external material

- Production migrations 042–045 remain unapplied; this session changed staging
  only.
- B5 needs the own-damage policy before exact FDW terms can be published.
- B6/B7/Gate 0 require the broker or counsel; F1 requires the fleet records.
- E5 is with Papaki/grserver; E6 certificate reissue must finish before E10 CAA.
- E8 needs Search Console exports. E1 and the remaining hosted §8 vendor/Sentry
  checks need the relevant dashboards or sandbox credentials.
- The maintained contract template fixes Article 4(f), but existing printed
  stock must be replaced before use.
